/** Surtitre : une pastille abricot et un mot. Remplace les libellés en capitales. */
export function Eyebrow({ children }: { children: React.ReactNode }) {
  return <p className="eyebrow rise">{children}</p>;
}
